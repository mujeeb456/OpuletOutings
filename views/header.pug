
header.header 
        nav.nav.nav--tours
            a.nav__el(href='/') All tours 
        .header_logo-right 
            img(src='/img/favicongold.png',alt='Logo',height="50px",width='50px')
        .header__logo
            //- p OpulentOutings 
            img(src='/img/brand-name.png', alt="Name", height="90px", width="290px")

        nav.nav.nav--user
            if user
               a.nav__el.nav__el--logout  Logout
               a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo ||'default-user.jpg'}`, alt=`Photo ${user.name}`)
                //- img.nav__user-img(src=`/img/user${user.photo}` alt=`PHoto ${user.name}`)
                span= user.name.split(' ')[0]
            else
               a.nav__el(href='/login') Log in
               a.nav__el.nav__el--cta(href='/signup') Sign up


